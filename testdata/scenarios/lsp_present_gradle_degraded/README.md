# lsp_present_gradle_degraded

- Includes Gradle files and ProbeJS metadata to simulate an indexed workspace.
- Used to model cases where metadata exists but the live Gradle service may still degrade.
