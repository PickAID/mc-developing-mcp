# prism_probe_instance

- Provides a PrismLauncher-style instance tree with one instance: `LostCivilization`.
- Includes read-only Probe inputs under `minecraft/.probejs/manifest.json` and `minecraft/kubejs/probe/probe.d.ts`.
