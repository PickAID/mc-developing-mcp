{
  "chunk_id": "chunk_008",
  "version": "1.20.1",
  "loader": "forge",
  "category": "events",
  "source_file": "net/minecraftforge/event/TickEvent.java",
  "symbols": ["TickEvent", "ServerTickEvent", "ClientTickEvent", "WorldTickEvent", "PlayerTickEvent"],
  "coverage_status": "complete",
  "description": "Tick events for server, client, world, and player",
  "code_example": "// Server tick event - runs every server tick\nMinecraftForge.EVENT_BUS.addListener(this::onServerTick);\n\nprivate void onServerTick(ServerTickEvent event) {\n    if (event.phase == TickEvent.Phase.END) {\n        // Code to run at end of server tick\n    }\n}\n\n// Player tick event - runs for each player each tick\nMinecraftForge.EVENT_BUS.addListener(this::onPlayerTick);\n\nprivate void onPlayerTick(PlayerTickEvent event) {\n    if (event.phase == TickEvent.Phase.END && event.player instanceof ServerPlayer) {\n        ServerPlayer player = (ServerPlayer) event.player;\n        // Custom player tick logic\n    }\n}\n\n// World tick event\nMinecraftForge.EVENT_BUS.addListener(this::onWorldTick);\n\nprivate void onWorldTick(WorldTickEvent event) {\n    if (event.phase == TickEvent.Phase.START && !event.world.isClientSide()) {\n        // World tick logic for server-side world\n    }\n}\n\n// Client tick event\nMinecraftForge.EVENT_BUS.addListener(this::onClientTick);\n\nprivate void onClientTick(ClientTickEvent event) {\n    if (event.phase == TickEvent.Phase.END) {\n        // Client tick logic\n    }\n}"
}
